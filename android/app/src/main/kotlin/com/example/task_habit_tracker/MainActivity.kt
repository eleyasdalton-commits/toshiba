package com.example.task_habit_tracker

// NOTE: `flutter create .` normally generates this file already. It's
// included here only so the widget provider has something to launch via
// HomeWidgetLaunchIntent — if your project already has a MainActivity.kt,
// keep the existing one (and update the widget provider's import if your
// package differs) instead of overwriting it with this copy.

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity()
